{
  "name": "aiu-portfolio-blockchain",
  "version": "1.0.0",
  "description": "Blockchain layer for AIU Student Portfolio System",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "node": "hardhat node",
    "compile": "hardhat compile",
    "clean": "hardhat clean"
  },
  "keywords": ["blockchain", "ethereum", "credentials", "education"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.19.0"
  }
}
